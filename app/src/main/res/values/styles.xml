<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="fareed2" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowAnimationStyle">@style/fareed</item>
    </style>

    <!-- Yeh animation ki style hai -->
    <style name="fareed">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_up</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_down</item>
    </style>
    <style name="CustomShapeStyle">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

</resources>